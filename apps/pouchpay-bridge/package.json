{
  "name": "pouchpay-bridge",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "PouchPay / ALLTRA route bridge — fills missing swap callData via UniswapV2 router",
  "scripts": {
    "start": "node src/server.mjs",
    "dev": "node --watch src/server.mjs",
    "test": "node --test test/bridge.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
